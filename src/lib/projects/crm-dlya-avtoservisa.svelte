<script module>
	export const metadata = {
		title: 'CRM для сети автосервисов',
		description:
			'Учёт заявок, история обслуживания клиентов и склад запчастей — вместо таблиц в Excel, которые расходились между филиалами.',
		date: '2026-07-10',
		published: true,
		category: 'crm',
		link: 'https://example.com',
		stack: ['Kotlin', 'Spring Boot', 'PostgreSQL', 'SvelteKit']
	};
</script>

<script>
	import { CodeBlock, PostTable, PostImage, PostList } from '$lib';
</script>

<p>
	Заказчик — сеть из трёх автосервисов, которая вела заявки в общей Excel-таблице. При росте
	числа мастеров и филиалов таблица начала расходиться: два человека одновременно правили один
	файл, история обслуживания терялась при смене мастера.
</p>

<h2>Задача</h2>

<PostList
	items={[
		'Единая база клиентов и истории обслуживания по всем филиалам',
		'Учёт остатков запчастей на складе с автосписанием при закрытии заявки',
		'Разграничение доступа — мастер видит только свои заявки, администратор — все',
		'Работа на слабом VPS без отдельного сервера под базу данных'
	]}
/>

<PostImage
	src="https://picsum.photos/seed/crm-avtoservis/1200/630"
	alt="Интерфейс CRM для автосервиса — список заявок"
	caption="Список заявок с фильтром по филиалу и статусу"
/>

<h2>Модель данных</h2>

<p>
	Заявка — корень агрегата: позиции работ и списанные запчасти живут внутри неё и не имеют
	смысла отдельно от заявки.
</p>

<CodeBlock
	code={`@Table("service_orders")
class ServiceOrder(
    @Id val id: Long? = null,
    val clientId: Long,
    val branchId: Long,
    var status: OrderStatus,
    @MappedCollection(idColumn = "order_id")
    val items: MutableList<ServiceOrderItem> = mutableListOf()
)`}
	language="kotlin"
	filename="ServiceOrder.kt"
/>

<h2>Результат</h2>

<PostTable
	headers={['Метрика', 'До', 'После']}
	rows={[
		['Время на поиск истории клиента', '5–10 минут', '~10 секунд'],
		['Расхождения данных между филиалами', 'еженедельно', 'исключены'],
		['Списание запчастей', 'вручную, задним числом', 'автоматически при закрытии заявки']
	]}
/>

<p>
	Проект развёрнут на том же VPS 2 ГБ, что и у остальных клиентов, — без выделенного сервера
	под базу: JVM-heap и пул соединений настроены под совместное использование ресурсов.
</p>
